from django.urls import path
from . import views

urlpatterns = [
    # path('hello/', views.hello, name='hello'),  # Маршрут для hello
    # path('select/', views.database),
    # path('insert/', views.add_and_delete_records),
    # path('<int:test_param>/', views.database_param_digit),
    # path('<str:test_param>/', views.database_param, name='str-param'),
]